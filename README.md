# 🔐 Encrypted Notes App

Веб-приложение для создания и управления зашифрованными заметками на PHP.

##  Возможности

-  Создание зашифрованных заметок
-  Просмотр с вводом пароля
-  Удаление заметок
-  Шифрование AES-256-CBC
-  Хранение в SQLite/MySQL

##  Технологии

- PHP 8.4+
- SQLite3 / MySQL
- OpenSSL для шифрования
- Bootstrap 5 для интерфейса
- PDO для работы с БД

### Алгоритм шифрования
- **Алгоритм:** AES-256-CBC
- **Длина ключа:** 256 бит
- **Режим:** CBC с уникальным IV для каждой заметки
- **Библиотека:** OpenSSL

### Как это работает?
1. **При создании заметки:**
   - Генерируется случайный IV (16 байт)
   - Текст шифруется с использованием мастер-пароля
   - IV и зашифрованный текст сохраняются в БД

2. **При просмотре заметки:**
   - Вводится мастер-пароль
   - Данные расшифровываются с использованием IV из БД
   - Если пароль неверен - показывается ошибка

### Важные детали безопасности:
-  Мастер-пароль НЕ сохраняется в системе
-  Каждая заметка имеет уникальный IV
-  Без пароля данные остаются зашифрованными
-  Потеря пароля = невозможность восстановления заметок

### Технические детали:
```sql
-- Структура данных в БД
notes (
    id INT,
    title TEXT,              -- Не шифруется
    encrypted_content TEXT,  -- Зашифрованный текст (Base64)
    iv TEXT,                 -- Вектор инициализации (Base64)
    created_at DATETIME
)
##  Установка (хотя зачем она, ну если вдруг надо)

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш-логин/encrypted-notes-app.git
cd encrypted-notes-app